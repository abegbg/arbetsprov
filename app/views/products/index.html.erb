<h1><%= @products.size %> Products</h1>
<% @products.each do |product| %>
	<article>
		<header>
			<h2><%= product.name %></h2>
		</header>
		<p>
			<%= product.description %>
		</p>
		<p>
			<%= number_to_currency(product.price, :locale => :sv) %>
		</p>
		<%= format_stock(product) %> 
		<p>
			Bild: <%= product.image %>
		</p>

		Knapp för att lägga till produkt till varukorgen.
		<br />
		Kanske kunna välja antal. BAPP

	</article>
<% end %>

