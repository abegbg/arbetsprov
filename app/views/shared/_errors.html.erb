<div id="error_explanation" style='display:none;'>
  <section id="errors">
    <header>
      <h2>
        <%= object.class.name.titleize.downcase %> kunde inte sparas. <!-- Vet inte hur man skall få rätt namn på denna. -->
      </h2>
      <h3>
        Justera följande fel:
      </h3>
    </header>
    <ul class="error">
      <li class="error">BAPP</li>

      <% if object %>
        <% object.errors.full_messages.each do |message| %>
          <li class="error"><%= message %></li>
        <% end %>
      <% end %>
    </ul>
  </section>
</div>


<div id="response_explanation" class="flash notice" style='display:none;'>bapp
  <header>
    <h2>
      <%= object.class.name.titleize.downcase %> har sparats. 
    </h2>
  </header>
</div>


<!-- 
      $error_container_ul = $("ul", $error_container)
    if $("li", $error_container_ul).length
      $("li", $error_container_ul).remove()
      $error_container.hide()
       -->